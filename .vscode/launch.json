{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PetGrow (Vercel Dev)",
      "type": "node-terminal",
      "request": "launch",
      "command": "vercel dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
